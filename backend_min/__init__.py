"""
Minimal FastAPI backend package for Railway boot testing.

This is intentionally tiny and dependency-light so that if this service
boots successfully on Railway while the main backend fails, the delta
between them highlights the failing imports or initialization logic.
"""


